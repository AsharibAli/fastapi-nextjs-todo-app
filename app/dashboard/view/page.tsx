import { Metadata } from "next";

export const metadata: Metadata = {
  title: "View Todos",
};

export default function Page() {
  return <p>View All Todos Page | Currently In Building phase.</p>;
}
